import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    text: {fontSize:30 , padding : 10 , backgroundColor:'blue', 
      borderColor:'lightgreen', borderWidth: 10, marginVertical:10, 
      color:'white'}
  })

export default styles